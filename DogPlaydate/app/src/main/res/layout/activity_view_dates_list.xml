<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="project.coen268.scu.dogplaydate.ViewDatesList"
    android:orientation="vertical">

    <TextView android:text="Current Record"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/currentDatesTextView"
        android:layout_margin="5dp"
        android:textStyle="bold|italic"
        android:textSize="15dp" />

    <ListView
        android:layout_width="match_parent"
        android:layout_height="220dp"
        android:id="@+id/currentDatesListView"
        android:scrollbars="vertical|horizontal"
        android:paddingBottom="2dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="History Record  "
        android:id="@+id/pastDatesTextView"
        android:singleLine="true"
        android:layout_margin="5dp"
        android:textStyle="bold|italic"
        android:textSize="15dp" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="220dp"
        android:id="@+id/pastDatesListView" />

</LinearLayout>
